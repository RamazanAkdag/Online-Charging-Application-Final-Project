<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean class="org.apache.ignite.configuration.IgniteConfiguration">

        <!-- IPv4 kullanımını zorunlu kıl -->
        <property name="jvmOptions">
            <list>
                <value>-Djava.net.preferIPv4Stack=true</value>
            </list>
        </property>

        <!-- Discovery SPI ayarları -->
        <property name="discoverySpi">
            <bean class="org.apache.ignite.spi.discovery.tcp.TcpDiscoverySpi"/>
        </property>

        <!-- Communication SPI ayarları -->
        <property name="communicationSpi">
            <bean class="org.apache.ignite.spi.communication.tcp.TcpCommunicationSpi"/>
        </property>

        <!-- Bağlantı zaman aşımı ayarları -->
        <property name="failureDetectionTimeout" value="5000"/>
        <property name="clientFailureDetectionTimeout" value="10000"/>
    </bean>

</beans>
