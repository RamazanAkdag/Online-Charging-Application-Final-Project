LAMBDA_DIR=./environments/dev/lambda
ZIP_FILE=$(LAMBDA_DIR)/efs_backup_lambda.zip

build:
	@echo "📦 Lambda zip paketi oluşturuluyor..."
	cd $(LAMBDA_DIR) && zip -r efs_backup_lambda.zip index.py > /dev/null

plan: build
	terraform -chdir=environments/dev plan

apply: build
	terraform -chdir=environments/dev apply
