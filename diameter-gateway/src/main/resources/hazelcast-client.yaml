hazelcast-client:
  cluster-name: hazelcast-cluster
  network:
    cluster-members:
      - hazelcast-node1:5701  # Buradaki IP'yi Docker'daki servislere göre kontrol et
    smart-routing: false  # Eğer gereksiz bağlantıları önlemek istiyorsan
    connection-timeout: 10000  # 10 saniyelik zaman aşımı
    redo-operation: true
